spring:
  datasource:
    url: jdbc:mysql://localhost:3306/issue_tracker?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 0098

  sql:
    init:
      mode: always # 로컬에서만 개발 편의를 위해 매번 DB를 밀고 새로 만든다.

logging:
  level:
    org.springframework.jdbc.datasource.init: DEBUG # schema.sql 실행 과정을 보여줍니다
    org.springframework.jdbc.core.JdbcTemplate: DEBUG